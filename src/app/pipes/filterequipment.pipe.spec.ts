import { FilterEquipmentPipe } from './filterequipment.pipe';

describe('FilterEquipmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEquipmentPipe();
    expect(pipe).toBeTruthy();
  });
});
